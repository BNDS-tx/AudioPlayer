<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AudioPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#1B6CBF</item>
        <item name="colorPrimaryContainer">#86ADE7</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="colorSecondaryContainer">#CDD2D9</item>
        <item name="colorOnPrimaryContainer">#08457E</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.AudioPlayer.NoActionBar" parent="Theme.AudioPlayer">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AudioPlayer.MD3Button" parent="Theme.AudioPlayer" />

    <!-- 样式用于扩展状态的标题 -->
    <style name="ExpandedTitleTextAppearance" parent="Theme.AudioPlayer">
        <item name="android:textSize">40sp</item> <!-- 自定义字体大小 -->
        <item name="android:fontFamily">sans-serif-medium</item> <!-- 其他属性可选 -->
    </style>

    <style name="Widget.Material3.Button.Segmented">

    </style>
</resources>